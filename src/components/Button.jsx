'use client'

function Button() {
  return (
    <div>
        
    <button className="btn-container" onClick={()=> alert ('Agregado al carrito!')}>Comprar</button>

    </div>
  )
}

export default Button